import styled from "styled-components";

const StyledLoadingBoundary = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  color: var(--color-primary);
`;

export default StyledLoadingBoundary;
